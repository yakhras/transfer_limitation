<?xml version="1.0" encoding="utf-8" ?>

<odoo>
    <record id="view_company_form_custom_tab" model="ir.ui.view">
        <field name="name">res.company.form.inherit.custom.tab</field>
        <field name="model">res.company</field>
        <field name="inherit_id" ref="base.view_company_form"/>
        <field name="arch" type="xml">
            <!-- Target the notebook and append after the last page -->
            <xpath expr="//form/sheet/notebook/page" position="after">
                <page string="Bank Info">
                    <group>
                        <!-- Display company_bank_journal_ids as a many2many_checkboxes widget -->
                        <field name="company_bank_journal_ids" widget="many2many_checkboxes" domain="[('company_id', '=', id)]"/>
                    </group>
                </page>
            </xpath>
        </field>
    </record>
</odoo>
