<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- Initialize configuration for companies that have common cash flow accounts -->
        <function model="cash.flow.config" name="auto_suggest_setup"/>

        <!-- Generate dashboard records from configuration -->
        <function model="cash.flow.config" name="generate_all_dashboard_records"/>

    </data>
</odoo>