<?xml version="1.0" encoding="utf-8" ?>
<odoo>

    <record id="view_partner_form_einvoice" model="ir.ui.view">
        <field name="name">res.partner.form.einvoice</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="l10n_tr_account_einvoice.view_partner_form_einvoice"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='einvoice_registered_user']" position="before">
                <field name="property_is_printed_invoice"/>
            </xpath>
        </field>
    </record>

</odoo>